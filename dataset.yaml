# Paths to the image directories
path: /datashare/HW1/labeled_image_data

train: images/train
val: images/val

# Number of classes
nc: 3

# Class names (must match the order used in your dataset labels)
names: ['Empty','Tweezers','Needle_driver']